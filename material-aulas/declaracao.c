#include <stdio.h>

int main() {
    int x;        // declaração
    x = 10;       // atribuição depois
    int y = 20;   // declaração + inicialização
    printf("x = %d, y = %d\n", x, y);
    return 0;
}
